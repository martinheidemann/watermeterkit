# WaterMeterKit V1 - Core device config
# logger, api, ota, time, wifi er leveret eksternt

substitutions:
  device_name: watermeterkit
  friendly_name: WaterMeterKit
  software_version: "1.6"
  hardware_version: "V1"

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  name_add_mac_suffix: true
  project:
    name: "smarthomeshop.watermeterkit"
    version: ${software_version}

esp8266:
  board: esp01_1m
  restore_from_flash: true

switch:
  - platform: restart
    id: switch_restart
    name: "${friendly_name} Restart"

button:
  - platform: factory_reset
    name: "${friendly_name} Factory Reset"
    entity_category: config
    disabled_by_default: true

status_led:
  pin: GPIO12

output:
  - platform: esp8266_pwm
    id: output_led_green
    pin: GPIO14
  - platform: esp8266_pwm
    id: output_led_blue
    pin: GPIO16

light:
  - platform: monochromatic
    id: light_led_green
    output: output_led_green
  - platform: monochromatic
    id: light_led_blue
    output: output_led_blue

i2c:
  sda: GPIO04
  scl: GPIO05

sensor:
  - platform: pulse_meter
    id: sensor_pulse_meter
    name: "${friendly_name} Current Usage"
    icon: "mdi:water-pump"
    unit_of_measurement: "L/min"
    internal_filter_mode: PULSE
    timeout: 2min
    internal_filter: 200ms
    accuracy_decimals: 1
    pin: GPIO13
    total:
      id: sensor_pulse_meter_total
      name: "${friendly_name} Total Consumption"
      icon: "mdi:cube-outline"
      unit_of_measurement: "m³"
      state_class: total_increasing
      device_class: water
      accuracy_decimals: 3
      filters:
        - multiply: 0.001
    on_value:
      then:
        - light.turn_on:
            id: light_led_green
            flash_length: 1s

  - platform: hdc1080
    temperature:
      id: sensor_temperature
      name: "${friendly_name} Temperature"
      filters:
        offset: -5.5
    humidity:
      id: sensor_humidity
      name: "${friendly_name} Humidity"
      filters:
        offset: 12
    update_interval: 60s

text_sensor:
  - platform: template
    id: text_sensor_software_version
    name: "${friendly_name} Software Version"
    update_interval: 5h
    lambda: 'return {"${software_version}"};'

  - platform: template
    id: text_sensor_hardware_version
    name: "${friendly_name} Hardware Version"
    update_interval: 5h
    lambda: 'return {"${hardware_version}"};'
